# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Бэкенд-эндпоинт (server/) — секреты только на сервере, не в репозитории
server/.env
server/node_modules
server/data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Генерируется перед каждой сборкой (см. package.json → "build" и
# scripts/generate-sitemap.mjs) — не коммитим, чтобы не плодить diff-шум
# на каждый PR, когда меняется список вакансий/мероприятий/статей.
public/sitemap.xml
